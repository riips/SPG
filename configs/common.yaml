hydra:
  job:
    num: 1
  job_logging:
    formatters:
      simple:
        format: '[%(asctime)s.%(msecs)03d][%(name)s][%(levelname)s]: %(message)s'
        datefmt: '%Y-%m-%d %H:%M:%S'

seed: 111

cache:
  dir: ${oc.env:HOME}/.cache/features
